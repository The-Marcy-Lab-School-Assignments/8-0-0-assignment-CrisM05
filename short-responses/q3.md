# Question

`app.get(endpoint, controller)` takes in a `controller` as the second argument.

What is a controller?

# Answer
A controller is the code that would be executed when the request is listened to.
# Teacher Feedback

X/3 points